# Packing the sweep output into arrays (convert.py).
numpy>=1.26
pandas>=2.0

# Neighbour search when building training graphs from the packed frames.
scipy>=1.11

# Model and training.
torch>=2.2
matplotlib>=3.8
